.DS_Store
node_modules/

# Local scratch copy of the data store. In CI this lives on the orphan `data`
# branch; locally it is regenerated by scripts/fetch.mjs and never committed
# to `main`.
/store/
/_site/
*.tmp
